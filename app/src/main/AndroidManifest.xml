<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AksharaDeepaTutor">

        <activity android:name=".activities.LoginActivity" android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.SplashActivity" android:exported="false"
            android:theme="@style/Theme.AksharaDeepaTutor.Splash"/>
        <activity android:name=".activities.MainActivity" android:exported="false"/>
        <activity android:name=".activities.SyllabusActivity" android:exported="false"/>
        <activity android:name=".activities.QuizActivity" android:exported="false"/>
        <activity android:name=".activities.StrengthMapActivity" android:exported="false"/>
        <activity android:name=".activities.ResultActivity" android:exported="false"/>
        <activity android:name=".activities.LeaderboardActivity" android:exported="false"/>
        <activity android:name=".activities.AnalyticsDashboardActivity" android:exported="false"/>

    </application>

</manifest>
